{
  "name": "ecs-image-inventory-lambda",
  "version": "1.0.0",
  "description": "Lambda that inventories ECS service container images across accounts/regions into an Excel workbook.",
  "main": "src/index.js",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp -r index.js package.json src dist/ && cd dist && npm install --omit=dev && npx -y bestzip ../lambda.zip *"
  },
  "dependencies": {
    "exceljs": "^4.4.0"
  },
  "devDependencies": {
    "@aws-sdk/client-ecs": "^3.658.0",
    "@aws-sdk/client-inspector2": "^3.1123.0",
    "@aws-sdk/client-s3": "^3.658.0",
    "@aws-sdk/client-ssm": "^3.658.0",
    "@aws-sdk/client-sts": "^3.658.0",
    "@aws-sdk/lib-storage": "^3.658.0"
  }
}
